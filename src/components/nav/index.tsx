import React from 'react';
import styled from 'styled-components';

const Nav: React.FC<{}> = () => {
  return (
    <NavBlock>
      
    </NavBlock>
  );
}

const NavBlock = styled.nav`
  width: 25%;
`;

export default Nav;